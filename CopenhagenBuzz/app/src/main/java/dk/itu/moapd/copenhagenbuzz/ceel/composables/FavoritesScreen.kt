package dk.itu.moapd.copenhagenbuzz.ceel.composables

import androidx.compose.foundation.background
import androidx.compose.foundation.layout.fillMaxSize
import androidx.compose.foundation.lazy.LazyColumn
import androidx.compose.runtime.Composable
import androidx.compose.ui.Modifier
import androidx.compose.ui.graphics.Color
import dk.itu.moapd.copenhagenbuzz.ceel.data.DataViewModel

/*@Composable
fun FavoritesScreen(viewModel: DataViewModel) {
    val events by viewModel.favoriteEvents.collectAsState()

    LazyColumn(
        modifier = Modifier
            .fillMaxSize()
            .background(Color(0xFFFDFDFD)) // optional
    ) {
        items(events) { event ->
            FavoriteItem(event)
        }
    }
}*/

